# Tasks: Fix All Dependabot Security Vulnerabilities

**Input**: Design documents from `/specs/814-fix-dependabot-cves/`
**Prerequisites**: plan.md (required), spec.md (required for user stories)

**Tests**: No new tests required — this is a dependency maintenance change. Existing test suite serves as regression validation.

**Organization**: Tasks grouped by user story to enable independent implementation and testing.

**Status**: ✅ All 11 tasks complete. 121 tests ran (1 pre-existing failure, 0 new failures). Zero CVEs remaining.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)
- Include exact file paths in descriptions

## Path Conventions

- **Dependency manifest**: `pyproject.toml`
- **Lock file**: `uv.lock` (regenerated by `uv lock`)

---

## Phase 1: User Story 1 — Resolve High Severity CVEs (Priority: P1) 🎯 MVP

**Goal**: Eliminate all 7 HIGH severity CVEs across `cryptography`, `wheel`, and `mcp[cli]` packages.

**Independent Test**: Run CVE scan on the updated dependency list and verify zero HIGH/CRITICAL findings. Run `uv lock` successfully.

### Implementation for User Story 1

- [x] T001 [P] [US1] Raise `cryptography` constraint from `>=44.0.0` to `>=46.0.7` in `pyproject.toml` line 44, `[project] dependencies` section. Resolves CVE-2024-12797, CVE-2026-26007 (HIGH), CVE-2026-34073, CVE-2026-39892 (MEDIUM). Lock file updated from 46.0.6 → 46.0.7. *(Plan Phase 1a, FR-001)*

- [x] T002 [P] [US1] Update `wheel` from `==0.45.1` to `>=0.46.2` in `pyproject.toml` line 56, `[project.optional-dependencies] test` section. Resolves CVE-2026-24049 (HIGH). Lock file updated from 0.45.1 → 0.46.3. *(Plan Phase 2a, FR-003)*

- [x] T003 [P] [US1] Update `wheel` from `==0.45.1` to `>=0.46.2` in `pyproject.toml` line 64, `[project.optional-dependencies] dev` section. Consistent with T002. *(Plan Phase 2b, FR-006)*

- [x] T004 [P] [US1] Update `wheel` from `==0.45.1` to `>=0.46.2` in `pyproject.toml` line 103, `[tool.uv] dev-dependencies` section. Consistent with T002 and T003. *(Plan Phase 2c, FR-006)*

- [x] T005 [P] [US1] Raise `mcp[cli]` constraint from `>=1.7.1` to `>=1.23.0` in `pyproject.toml` line 74, `[project.optional-dependencies] mcp` section. Resolves CVE-2025-53365 (HIGH), CVE-2025-53366 (HIGH), CVE-2025-66416 (HIGH). Lock file updated from 1.19.0 → 1.27.0. *(Plan Phase 3a, FR-004)*

**Checkpoint**: ✅ All HIGH severity CVE constraints updated.

---

## Phase 2: User Story 2 — Resolve Medium Severity CVEs (Priority: P2)

**Goal**: Eliminate all 3 MEDIUM severity CVEs in `jinja2`.

**Independent Test**: Run CVE scan on `jinja2` and verify zero MEDIUM findings.

### Implementation for User Story 2

- [x] T006 [P] [US2] Raise `jinja2` constraint from `>=2.10` to `>=3.1.5` in `pyproject.toml` line 33, `[project] dependencies` section. Resolves CVE-2020-28493, CVE-2024-22195, CVE-2024-56326 (all MEDIUM). Lock file already at 3.1.6 — constraint floor update only. *(Plan Phase 1b, FR-002)*

**Checkpoint**: ✅ All version constraints updated.

---

## Phase 3: User Story 3 — Verify No Functional Regressions (Priority: P3)

**Goal**: Regenerate lock file, install updated dependencies, and confirm all existing tests pass.

**Independent Test**: Run full test suite, verify clean installation, and confirm critical imports work.

### Implementation for User Story 3

- [x] T007 [US3] Regenerate lock file by running `uv lock` from project root. Resolved: `cryptography` 46.0.7, `jinja2` 3.1.6, `wheel` 0.46.3, `mcp` 1.27.0. Zero conflicts. *(Plan Phase 4, FR-007)*

- [x] T008 [US3] Install updated dependencies with `uv pip install -e ".[dev,mcp]"` — clean installation, zero errors. *(Plan Phase 5b, SC-003)*

- [x] T009 [US3] Run existing test suite — 121 tests ran, 1 pre-existing failure (commented-out warning in encrypted backend), 0 new failures from dependency updates. *(Plan Phase 5c, SC-002)*

- [x] T010 [US3] Import smoke test: `cryptography` 46.0.7 OK (`default_backend`, `AESGCM`, `PBKDF2HMAC`), `jinja2` 3.1.6 OK, `wheel` 0.46.3 OK. *(Plan Phase 5d)*

- [x] T011 [US3] CVE scan: **zero known CVEs** across all declared dependencies. *(Plan Phase 5a, FR-008, SC-001)*

**Checkpoint**: ✅ All dependencies updated, lock file regenerated, tests pass, zero CVEs remain.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (US1 — HIGH CVEs)**: No dependencies — start immediately. All T001–T005 are parallel ([P]) since they edit different lines in the same file with no overlap.
- **Phase 2 (US2 — MEDIUM CVEs)**: No dependency on Phase 1 — can run in parallel. T006 edits a different line.
- **Phase 3 (US3 — Verification)**: Depends on Phases 1 and 2 — all constraint changes must be complete before lock file regeneration.

### Within Each Phase

- Phase 1: All 5 tasks are independent and can be applied in any order (different lines in `pyproject.toml`)
- Phase 2: Single task, no internal dependencies
- Phase 3: Sequential — T007 (lock) → T008 (install) → T009 (tests) → T010 (smoke test) → T011 (CVE scan)

### Parallel Opportunities

```
Phase 1: T001, T002, T003, T004, T005  ─┐  All [P] — edit different
Phase 2: T006                            ├─ lines in pyproject.toml
                                        ─┘
                    │
                    ▼
Phase 3: T007 → T008 → T009 → T010 → T011  (sequential)
```

---

## Implementation Strategy

### MVP First (Phase 1 Only)

1. Apply T001–T005 to `pyproject.toml` (all HIGH CVE fixes)
2. Run `uv lock` and verify
3. **STOP and VALIDATE**: This alone eliminates 7 of 10 CVEs and all HIGH severity issues

### Full Delivery

1. Phase 1 (T001–T005) → HIGH CVEs fixed
2. Phase 2 (T006) → MEDIUM CVEs fixed
3. Phase 3 (T007–T011) → Lock file regenerated, verified, zero CVEs confirmed
4. Total: 11 tasks across 3 phases

---

## Notes

- [P] tasks = different lines in the same file, no overlap — safe to apply in any order or as a single batch edit
- All Phase 1 and Phase 2 tasks edit `pyproject.toml` only — can be combined into a single commit
- Phase 3 tasks are verification steps — T007 (`uv lock`) is the only one that creates a file change
- No source code (`.py`) files are modified in this feature
- Total: 11 tasks across 3 phases

